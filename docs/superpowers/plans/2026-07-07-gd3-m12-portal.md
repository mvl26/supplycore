# GĐ3 — M12 Portal Khách hàng (Cô lập dữ liệu) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. TDD. Steps `- [ ]`. RSK-01 (rò rỉ dữ liệu chéo) là rủi ro cao nhất — viết test cô lập TRƯỚC.

**Goal:** Xây ranh giới bảo mật backend cho Portal khách hàng: mỗi khách chỉ thấy dữ liệu của mình; chặn khách khỏi dữ liệu/API nội bộ; 7 API portal + theo dõi 4 cột mốc.

**Architecture:** permission_query_conditions + has_permission (hooks) cho 5 doctype bán (đều có field `customer`→SC Customer.portal_user), role `SC Customer Portal` (desk_access=0), API `api/portal.py` tự lọc theo `frappe.session.user`. UI để GĐ4.

**Tech Stack:** Frappe v15 thuần.

## Global Constraints
- Nhánh `feat/mvl-distributor`. Site `supplycore-miyano.local`. Bench `/home/hoangvietyeuem/frappe-bench-yhct`. Pkg `.../apps/supplycore/supplycore`.
- INVARIANT sau mỗi task: `cd /home/hoangvietyeuem/frappe-bench-yhct && bench --site supplycore-miyano.local migrate` sạch + `frappe.ping`→pong.
- TDD: test đỏ trước. Test cô lập mirror `supplycore/tests/uc16_test.py` (save original user, set User.roles, `frappe.set_user`, try/finally khôi phục + rollback). Run: `bench --site supplycore-miyano.local execute supplycore.tests.<name>.run`.
- Permission_query pattern mirror `supplycore/utils/permissions.py::stock_entry_query(user=None)->str` (khai ở hooks.py:80). API role-gate pattern mirror `supplycore/api/frontend.py:642` (`frappe.get_roles` + set-check + `frappe.PermissionError`).
- 5 doctype đều có field `customer` (Link SC Customer); SC Customer có `portal_user` (Link User).
- cwd reset sau bench — absolute path. Commit trailer: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
- KHÔNG cấp permission cho role Portal trong bất kỳ doctype JSON nội bộ. UI để GĐ4.

---

### Task 1: Role Portal + provisioning + BRU-CUS-001
**Files:** Create `supplycore/patches/v0_9/{__init__.py, create_portal_role.py}`; Modify `supplycore/patches.txt`, `supplycore/install.py` (DEFAULT_ROLES += "SC Customer Portal"), `supplycore/m7_sales/doctype/sc_customer/sc_customer.py`; Create `supplycore/api/portal.py` (chỉ hàm provision ở task này); Create `supplycore/tests/portal_provision_test.py`.
- Patch `create_portal_role.py`: idempotent tạo Role "SC Customer Portal" với `desk_access=0` (nếu chưa có).
- `sc_customer.py validate()`: nếu `self.status=="Hoạt động"` và không `self.portal_user` → `frappe.throw` (BRU-CUS-001: phải có tài khoản Portal trước khi kích hoạt). (Giữ BRU-CUS-002 1-1 đã có.)
- `api/portal.py::portal_provision(customer, email)`: role-gate (chỉ System Manager/SupplyCore Manager/SupplyCore Purchaser); tạo hoặc lấy `User` (user_type="Website User", email, first_name=customer_name); thêm role "SC Customer Portal"; set `SC Customer.portal_user=user`; return user. Không desk access.

- [ ] **Step 1 (RED):** `portal_provision_test.py`: `test_portal_role_exists()` (sau migrate assert Role tồn tại), `test_activate_customer_without_portal_blocked()` (customer status Hoạt động, portal_user rỗng → throw BRU-CUS-001), `test_provision_creates_website_user_and_links()` (gọi portal_provision → User có role Portal, customer.portal_user set). Run → FAIL.
- [ ] **Step 2 (GREEN):** patch + install + sc_customer validate + portal_provision. `bench migrate`. Run → PASS.
- [ ] **Step 3:** migrate+ping. **Commit** `feat(mvl-portal): role SC Customer Portal + provisioning + BRU-CUS-001`

---

### Task 2: Cô lập dữ liệu (RSK-01) — permission_query + has_permission [REVIEW]
**Files:** Modify `supplycore/utils/permissions.py` (thêm `portal_customer_query(user)` + `portal_doc_permission(doc, user, permission_type)`); Modify `supplycore/hooks.py` (đăng ký permission_query_conditions + has_permission cho 5 doctype); Create `supplycore/tests/portal_isolation_test.py`.
- `portal_customer_query(user=None)`: `user=user or frappe.session.user`; roles=frappe.get_roles(user); nếu "SC Customer Portal" NOT in roles → return "" (nội bộ không giới hạn); else return `"`tab{doctype}`... "` — dùng chữ ký Frappe truyền doctype? (Frappe gọi permission_query_conditions với chỉ `user`; SQL phải tự biết bảng. Frappe áp WHERE vào bảng chính, nên trả `` `tabSC Sales Order`.customer in (select name from `tabSC Customer` where portal_user = {frappe.db.escape(user)}) `` — viết 1 hàm per-doctype hoặc 1 factory. Đơn giản: 5 hàm mỏng gọi 1 helper `_customer_scope_sql(table, user)`.)
- `portal_doc_permission(doc, user=None, permission_type=None)`: nếu "SC Customer Portal" in roles → chỉ True khi `permission_type in (None,"read")` AND doc.customer thuộc customer của user; nội bộ → True.
- hooks: `permission_query_conditions` += 5 doctype (SC Sales Order, SC Delivery Note, SC Sales Invoice, SC Sales Receipt, SC Sales Framework Contract); `has_permission` = {5 doctype: portal_doc_permission}.

- [ ] **Step 1 (RED):** `portal_isolation_test.py` — helper tạo 2 khách A,B (mỗi khách 1 website user + role Portal), mỗi khách có 1 SC Sales Order (+ chain tối thiểu). Tests:
  - `test_portal_A_cannot_list_B_orders()`: `frappe.set_user(A_user)`; `frappe.get_list("SC Sales Order")` chỉ chứa đơn của A, không có B.
  - `test_portal_A_cannot_getdoc_B_order()`: `frappe.set_user(A_user)`; `frappe.get_doc("SC Sales Order", B_order)` với check permission → PermissionError (dùng `frappe.has_permission` hoặc get_doc rồi check).
  - `test_portal_blocked_internal_doctype()`: `frappe.set_user(A_user)`; `frappe.get_list("SC Supplier")` → rỗng/deny và `frappe.has_permission("SC Purchase Order","read")` False.
  - `test_internal_user_sees_all()`: set Manager user → get_list SC Sales Order thấy cả A và B (không hồi quy nội bộ).
  Run → FAIL (chưa đăng ký).
- [ ] **Step 2 (GREEN):** implement + đăng ký hooks. `bench migrate`. Run → PASS.
- [ ] **Step 3:** migrate+ping. **Commit** `feat(mvl-portal): cô lập dữ liệu khách (permission_query + has_permission, RSK-01)`
- [ ] **Step 4: REVIEW** — task này được review riêng (bảo mật).

---

### Task 3: 7 API Portal (`api/portal.py`) [REVIEW]
**Files:** Modify `supplycore/api/portal.py`; Create `supplycore/tests/portal_api_test.py`.
Helper `_require_portal_customer()`: roles=get_roles(session.user); nếu "SC Customer Portal" not in roles → PermissionError; tìm SC Customer portal_user==session.user; nếu không → PermissionError; return customer name. Mỗi API bên dưới gọi helper rồi lọc theo customer:
- `portal_me()`: hồ sơ khách + credit_limit + công nợ (`SCGLEntry.get_balance("131", customer)`).
- `portal_contracts()`: SFC "Hiệu lực" của khách + items.
- `portal_catalog()`: gộp items từ SFC còn hiệu lực (item, uom, unit_price, remaining_qty).
- `portal_order_place(contract, items)`: tạo SC Sales Order cho khách (ignore_permissions insert vì khách không có create; controller vẫn enforce BRU-SFC/SO). Trả order name + total.
- `portal_order_track(order)`: nếu order.customer != khách → PermissionError; trả 4 cột mốc (Task 4 cung cấp hàm `compute_milestones`).
- `portal_order_history(limit=20, start=0)`: đơn của khách.
- `portal_document_download(doctype, name)`: chỉ doctype in ("SC Sales Invoice","SC Delivery Note"); nếu doc.customer != khách → PermissionError; trả print/pdf url hoặc html (`frappe.get_print`).

- [ ] **Step 1 (RED):** `portal_api_test.py` (set_user portal): mỗi API happy-path trả dữ liệu của khách; negative: `test_track_other_customer_order_denied()`, `test_document_download_other_customer_denied()`, `test_portal_api_by_internal_user_denied()` (internal user không map customer → PermissionError). `portal_order_place` tạo đơn Chờ duyệt đúng giá HĐ. Run → FAIL.
- [ ] **Step 2 (GREEN):** implement. `bench migrate`. Run → PASS.
- [ ] **Step 3:** migrate+ping. **Commit** `feat(mvl-portal): 7 API portal khách (tự lọc theo khách đăng nhập)`
- [ ] **Step 4: REVIEW** — review riêng (bảo mật + cô lập).

---

### Task 4: 4 cột mốc + E2E portal + regression
**Files:** Modify `supplycore/api/portal.py` (hàm `compute_milestones(order_name)`); Create `supplycore/tests/portal_e2e_test.py`.
- `compute_milestones(order)`: trả list 4 mốc:
  1. `placed` — SO tồn tại (done; time=order_date/creation).
  2. `delivered_accepted` — có DN của SO đã "Đã nghiệm thu" (done + time) hoặc pending.
  3. `invoiced` — có SI phát hành cho DN đó (done + time).
  4. `paid` — SI outstanding<=0 ("Đã thu đủ").
  Mỗi mốc: {key, label, status: done|current|pending, time}. `current` = mốc pending đầu tiên.

- [ ] **Step 1 (RED):** `portal_e2e_test.py::test_portal_milestones_progress()`: dựng chuỗi 1 khách qua từng bước, gọi `portal_order_track` (as portal user) và assert số mốc `done` tăng: sau SO=1, sau nghiệm thu=2, sau SI=3, sau thu đủ=4. Run → FAIL.
- [ ] **Step 2 (GREEN):** implement compute_milestones + wire vào order_track. Run → PASS.
- [ ] **Step 3 (regression):** `bench migrate` + `bench build --app supplycore` sạch; ping; chạy lại: portal_provision/isolation/api tests + GĐ2 sales suites + smoke_m8/m10 + uc26 → pass; nội bộ get_list SC Sales Order (Manager) vẫn thấy hết; code-grep GĐ1 hospital vẫn 0.
- [ ] **Step 4: Commit** `test(mvl-portal): 4 cột mốc + E2E portal + regression GĐ3`

---

## Định nghĩa Done (GĐ3)
- [ ] Role Portal + provisioning + BRU-CUS-001; cô lập 5 doctype (permission_query + has_permission); 7 API; 4 cột mốc.
- [ ] Bộ test cô lập ISO-1..4 pass; nội bộ không hồi quy (Manager thấy hết); GĐ2 suite pass.
- [ ] migrate/build/ping sạch. Commit trên `feat/mvl-distributor`. Sẵn sàng GĐ4 (UI portal + rebrand).
