# GĐ2 — M7 Bán hàng (Order-to-Cash) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. TDD: mỗi task viết test ĐỎ trước, code cho XANH, commit. Steps dùng checkbox `- [ ]`.

**Goal:** Dựng chuỗi bán hàng backend cho công ty phân phối MVL: Customer → Sales Framework Contract → Sales Order → Delivery Note (xuất kho) → Acceptance → Sales Invoice (GL phải thu) → Sales Receipt (thu tiền).

**Architecture:** Mirror chuỗi MUA sẵn có. Ghi kho qua `SCStockLedgerEntry.post()`, ghi sổ cái qua `SCGLEntry.post()/post_journal()/cancel_voucher()`. Workflow bằng field `status` + controller (không dùng Frappe Workflow engine — nhất quán GĐ1). Module mới `M7 Sales` (`supplycore/m7_sales/`).

**Tech Stack:** Frappe v15 thuần (no ERPNext), Python, MariaDB, bench.

## Global Constraints
- Nhánh `feat/mvl-distributor`. Site `supplycore-miyano.local`. Bench dir `/home/hoangvietyeuem/frappe-bench-yhct`. App pkg `.../apps/supplycore/supplycore`.
- Prefix DocType `SC `. autoname khai trong .json: `"autoname": "format:SC-XX-{YYYY}-{#####}"`, `"is_submittable": 1` cho chứng từ.
- INVARIANT sau mỗi task: `cd /home/hoangvietyeuem/frappe-bench-yhct && bench --site supplycore-miyano.local migrate` sạch + `bench --site supplycore-miyano.local execute frappe.ping` → `pong`.
- TDD bắt buộc: viết test đỏ trước (RED), rồi code (GREEN). Test pattern: file `supplycore/tests/<name>.py`, mỗi `test_*()` dùng `frappe.new_doc`, `flags.ignore_permissions=True`, `frappe.db.rollback()` cuối test; hàm `run()` loop list `tests` bắt exception trả `{passed,total,results}`. Chạy: `bench --site supplycore-miyano.local execute supplycore.tests.<name>.run`.
- cwd reset sau bench — dùng absolute path.
- Mỗi commit kết bằng: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
- KHÔNG làm Portal/M12, KHÔNG làm UI Vue (phase sau). Chỉ backend + Desk mặc định + API.

## Điểm MIRROR (đọc trước khi code)
- SLE: `SCStockLedgerEntry.post(item, warehouse, qty_change, voucher_type, voucher_no, *, batch, bin_location, valuation_rate, posting_date, voucher_detail_no, remarks)` — file `supplycore/doctype/sc_stock_ledger_entry/sc_stock_ledger_entry.py`. Xuất kho = `qty_change` ÂM. Cancel: chèn dòng đối ứng + `is_cancelled=1` (xem `SC Purchase Receipt._reverse_stock_ledger`).
- GL: `SCGLEntry.post(account,debit,credit,*,voucher_type,voucher_no,posting_date,party_type,party,against_account)`, `SCGLEntry.post_journal(entries,voucher_type,voucher_no,posting_date)`, `SCGLEntry.cancel_voucher(vt,vn)`, `SCGLEntry.get_balance(account,party)`. Mirror `SC Purchase Invoice._post_gl_entries()` (`m8_accounting/doctype/sc_purchase_invoice/sc_purchase_invoice.py`).
- Controller submittable: mirror `supplycore/doctype/sc_purchase_order/sc_purchase_order.py` (validate/before_submit/on_submit/on_cancel + status).
- FEFO: `supplycore.api.fefo.auto_pick_fefo(item_code, warehouse, qty)` → `{picked:[{batch_no,qty}], fully_satisfied, shortfall}`.
- Framework Contract remaining: mirror `m1_contract/doctype/framework_contract/framework_contract.py` (`recalculate_used_value`, FC Item.remaining_qty).
- GL accounts có sẵn: 131 Phải thu KH (Receivable), 3331 Thuế GTGT phải nộp, 632 Giá vốn, 156 Hàng hóa. **Thiếu 511 Doanh thu** → Task 1 seed.

---

### Task 1: Nền tảng — module M7 Sales, GL account 511, Settings fields
**Files:**
- Create: `supplycore/m7_sales/__init__.py`, `supplycore/m7_sales/doctype/__init__.py`
- Modify: `supplycore/modules.txt` (thêm dòng `M7 Sales`)
- Create: `supplycore/patches/v0_8/__init__.py`, `supplycore/patches/v0_8/seed_sales_gl_accounts.py`
- Modify: `supplycore/patches.txt` (append `supplycore.patches.v0_8.seed_sales_gl_accounts`)
- Modify: `supplycore/supplycore/doctype/supplycore_settings/supplycore_settings.json` (thêm field `fiscal_lock_date` Date + `default_receivable_account`/`default_revenue_account`/`default_tax_output_account`/`default_cash_account` Link SC GL Account, section "Bán hàng & Sổ cái")

- [ ] **Step 1 (RED):** Viết `supplycore/tests/gd2_foundation_test.py` với `test_revenue_account_exists()` (assert `frappe.db.exists("SC GL Account","511")`), `test_settings_has_fiscal_lock()` (assert field `fiscal_lock_date` tồn tại trên SupplyCore Settings), `test_m7_sales_module_registered()` (assert `"M7 Sales"` in modules.txt đọc file). Chạy `run` → FAIL.
- [ ] **Step 2:** Tạo module scaffold + modules.txt. Viết patch `seed_sales_gl_accounts.py`: idempotent tạo `SC GL Account` `511` (account_name "Doanh thu bán hàng", account_type Income, root_type Income) nếu chưa có (mirror `_seed_gl_accounts` trong seed_master_data.py). Thêm 4 field mapping + fiscal_lock_date vào Settings.json.
- [ ] **Step 3 (GREEN):** `bench migrate` (chạy patch) → `bench ... execute supplycore.tests.gd2_foundation_test.run` → PASS. `frappe.ping` → pong.
- [ ] **Step 4: Commit** `feat(mvl-sales): nền tảng M7 Sales — module, GL 511, Settings sổ bán hàng`

---

### Task 2: SC Customer (master)
**Files:** Create `supplycore/m7_sales/doctype/sc_customer/{sc_customer.json,sc_customer.py,__init__.py}`; Create `supplycore/tests/sc_customer_test.py`
**Interfaces:** Produces DocType `SC Customer` (module "M7 Sales"), autoname `format:SC-CUS-{#####}`.
Fields: `customer_code`(Data,read_only sau autoname? dùng autoname series), `customer_name`(reqd), `tax_code`(Data, unique=1), `billing_address`(Small Text), `shipping_address`(Small Text), `credit_limit`(Currency, default 0), `payment_terms`(Data), `status`(Select "Hoạt động\nTạm ngưng", default "Hoạt động"), `portal_user`(Link User). Permissions: System Manager, SupplyCore Manager, SupplyCore Purchaser (full CRUD); Accountant, Storekeeper, Auditor read.
Controller `validate()`: nếu `portal_user` set → đảm bảo không customer khác dùng cùng user (BRU-CUS-002).
- [ ] **Step 1 (RED):** `sc_customer_test.py`: `test_create_customer()`, `test_tax_code_unique()` (tạo 2 customer cùng tax_code → cái 2 raise), `test_portal_user_unique_if_set()` (2 customer cùng portal_user → raise). `run` → FAIL (doctype chưa có).
- [ ] **Step 2 (GREEN):** Tạo doctype json+py. migrate. `run` → PASS.
- [ ] **Step 3:** migrate+ping sạch. **Commit** `feat(mvl-sales): SC Customer master (BRU-CUS-002)`

---

### Task 3: SC Sales Framework Contract + SFC Item
**Files:** Create doctype `sc_sales_framework_contract` (submittable) + child `sfc_item`; Create `supplycore/tests/sc_sfc_test.py`
**Interfaces:** Produces `SC Sales Framework Contract` autoname `format:SC-SFC-{YYYY}-{#####}`, is_submittable; child `SFC Item`.
SFC fields: `contract_no`(autoname), `customer`(Link SC Customer, reqd), `valid_from`(Date), `valid_to`(Date), `total_value`(Currency, read_only), `status`(Select "Nháp\nChờ duyệt\nHiệu lực\nHết hạn\nThanh lý", default Nháp), `approved_by`(Link User, read_only), `items`(Table SFC Item).
SFC Item fields: `item`(Link SC Item, reqd), `uom`(Link SC UOM), `contract_qty`(Float, reqd), `unit_price`(Currency, reqd), `sold_qty`(Float, read_only, default 0), `remaining_qty`(Float, read_only). 
Controller: `validate()` compute total_value = Σ(contract_qty×unit_price), remaining_qty = contract_qty - sold_qty; `on_submit()` status="Hiệu lực"; method `recalculate_sold_qty()` (SUM SO Item.qty submitted theo contract+item) — gọi từ SO submit/cancel (mirror FC.recalculate_used_value); `_derive_status()` Hết hạn nếu quá valid_to.
- [ ] **Step 1 (RED):** `sc_sfc_test.py`: `test_create_submit_sfc()` (submit → status Hiệu lực, remaining_qty=contract_qty), `test_total_value_computed()`. `run` FAIL.
- [ ] **Step 2 (GREEN):** tạo doctype. migrate. `run` PASS.
- [ ] **Step 3:** migrate+ping. **Commit** `feat(mvl-sales): SC Sales Framework Contract + SFC Item`

---

### Task 4: SC Sales Order + SO Item (BRU-SFC-001/002, BRU-SO-001/002, BRU-AR-001)
**Files:** Create `sc_sales_order`(submittable) + child `so_item`; Modify `sc_sales_framework_contract.py` (add `recalculate_sold_qty` hook call target); Create `supplycore/tests/sc_sales_order_test.py`
**Interfaces:** Consumes SC Customer, SC Sales Framework Contract. Produces `SC Sales Order` autoname `format:SC-SO-{YYYY}-{#####}`.
SO fields: `order_no`, `customer`(Link, reqd), `framework_contract`(Link SC Sales Framework Contract, reqd), `order_date`(Date, default Today), `status`(Select "Chờ duyệt\nĐã duyệt\nĐang xử lý\nĐã bàn giao\nHoàn tất\nTừ chối", default "Chờ duyệt"), `approval_by`(Link User, read_only), `total_amount`(Currency, read_only), `credit_hold`(Check, read_only), `items`(Table SO Item).
SO Item: `item`(Link SC Item), `uom`, `qty`(Float,reqd), `unit_price`(Currency, read_only — fetch từ SFC Item), `amount`(Currency, read_only).
Controller `validate()`: với mỗi item → tìm SFC Item khớp (item ∈ SFC còn hiệu lực, else throw BRU-SFC-001); set unit_price = SFC Item.unit_price (KHÔNG cho sửa — BRU-SFC-002); check Σqty theo item ≤ SFC Item.remaining_qty (BRU-SO-001); compute amount + total_amount. `on_submit()`: BRU-AR-001 — nếu `SCGLEntry.get_balance("131", customer) + total_amount > customer.credit_limit` (và credit_limit>0) → set credit_hold + status giữ "Chờ duyệt" cần duyệt cấp cao (throw hoặc flag). Whitelisted `approve()` (role Purchaser/Manager) → status "Đã duyệt", cập nhật SFC.recalculate_sold_qty. `reject()` → "Từ chối". `on_cancel()` đảo sold_qty.
- [ ] **Step 1 (RED):** `sc_sales_order_test.py`: `test_price_from_sfc_not_editable()` (set unit_price khác → bị ghi đè bằng giá SFC), `test_item_not_in_sfc_blocked()` (BRU-SFC-001 raise), `test_qty_exceeds_remaining_blocked()` (BRU-SO-001 raise), `test_approve_sets_status_and_reduces_remaining()`, `test_credit_limit_exceeded_holds()` (BRU-AR-001). `run` FAIL.
- [ ] **Step 2 (GREEN):** tạo doctype + controller. migrate. `run` PASS.
- [ ] **Step 3:** migrate+ping. **Commit** `feat(mvl-sales): SC Sales Order + SO Item (BRU-SFC/SO/AR)`

---

### Task 5: SC Delivery Note + DN Item (xuất kho SLE, FEFO, BRU-SO-002/INV-001/EXP-001)
**Files:** Create `sc_delivery_note`(submittable) + child `dn_item`; Create `supplycore/tests/sc_delivery_note_test.py`
**Interfaces:** Consumes SC Sales Order, `SCStockLedgerEntry.post`, `supplycore.api.fefo.auto_pick_fefo`.
DN fields: `dn_no`, `sales_order`(Link, reqd), `customer`(fetch, read_only), `from_warehouse`(Link SC Warehouse, reqd), `delivery_date`(Date, default Today), `acceptance_ref`(Link SC Acceptance Record, read_only), `status`(Select "Nháp\nĐã giao\nĐã nghiệm thu\nĐã xuất HĐ", default "Nháp"), `items`(Table DN Item).
DN Item: `item`, `uom`, `qty`(Float,reqd), `batch`(Link SC Batch), `warehouse`(Link SC Warehouse).
Controller `validate()`: sales_order.status phải "Đã duyệt"/"Đang xử lý" (BRU-SO-002 — else throw); nếu batch trống → gọi auto_pick_fefo điền batch. `before_submit`: kiểm mỗi batch không hết hạn (BRU-EXP-001) và available ≥ qty (BRU-INV-001). `on_submit()`: mỗi item ghi `SCStockLedgerEntry.post(item, warehouse, qty_change=-qty, voucher_type="SC Delivery Note", voucher_no=name, batch=batch, valuation_rate=<lấy từ batch/SLE>)`; set status "Đã giao"; set sales_order.status "Đã bàn giao". `on_cancel()`: reverse SLE (mirror _reverse_stock_ledger) + revert SO status.
- [ ] **Step 1 (RED):** `sc_delivery_note_test.py`: `test_dn_from_unapproved_so_blocked()` (BRU-SO-002), `test_dn_submit_posts_negative_sle()` (SLE balance giảm đúng qty), `test_dn_expired_batch_blocked()` (BRU-EXP-001), `test_dn_insufficient_stock_blocked()` (BRU-INV-001), `test_dn_cancel_reverses_sle()`. Cần seed batch tồn tại (mirror uc test có tạo batch + PR nhập). `run` FAIL.
- [ ] **Step 2 (GREEN):** tạo doctype + controller. migrate. `run` PASS.
- [ ] **Step 3:** migrate+ping. **Commit** `feat(mvl-sales): SC Delivery Note — xuất kho FEFO + SLE âm (BRU-SO-002/INV/EXP)`

---

### Task 6: SC Acceptance Record (nghiệm thu)
**Files:** Create `sc_acceptance_record`(submittable); Create `supplycore/tests/sc_acceptance_test.py`
AR fields: `acceptance_no`, `delivery_note`(Link, reqd), `customer`(fetch), `acceptance_date`(Date, default Today), `accepted_by`(Data — tên người nhận phía khách), `note`(Small Text), `status`(Select "Nháp\nĐã nghiệm thu", default Nháp).
Controller `on_submit()`: set status "Đã nghiệm thu"; set delivery_note.status "Đã nghiệm thu" + delivery_note.acceptance_ref = name. `on_cancel()`: revert DN status "Đã giao", clear acceptance_ref.
- [ ] **Step 1 (RED):** `test_acceptance_marks_dn()` (submit AR → DN.status "Đã nghiệm thu", acceptance_ref set). `run` FAIL.
- [ ] **Step 2 (GREEN):** tạo doctype. migrate. `run` PASS.
- [ ] **Step 3:** migrate+ping. **Commit** `feat(mvl-sales): SC Acceptance Record (nghiệm thu → mở khóa xuất HĐ)`

---

### Task 7: SC Sales Invoice + SI Item (GL phải thu + COGS, BRU-DEL-001/INVC-001/PAY-001)
**Files:** Create `sc_sales_invoice`(submittable) + child `si_item`; Create `supplycore/tests/sc_sales_invoice_test.py`
**Interfaces:** Consumes SC Delivery Note (đã nghiệm thu), `SCGLEntry.post_journal`, Settings account mappings.
SI fields: `invoice_no`, `customer`(reqd), `delivery_note`(Link, reqd), `invoice_date`(Date, default Today), `total_amount`(Currency, read_only), `tax_rate`(Percent, default 0), `tax_amount`(Currency, read_only), `grand_total`(Currency, read_only), `outstanding_amount`(Currency, read_only), `status`(Select "Nháp\nĐã phát hành\nĐã thu một phần\nĐã thu đủ\nHủy", default Nháp), `items`(Table SI Item).
SI Item: `item`, `qty`, `unit_price`, `amount`.
Controller `validate()`: delivery_note.status phải "Đã nghiệm thu" (BRU-DEL-001, else throw); items phải khớp DN Item (item+qty) — BRU-INVC-001 (lệch → throw); compute total/tax/grand_total; outstanding = grand_total. `on_submit()`: BRU-PAY-001 nếu invoice_date ≤ Settings.fiscal_lock_date → throw; ghi GL qua `post_journal`: Dr 131 (party=customer) grand_total; Cr 511 total_amount; Cr 3331 tax_amount (nếu>0). Bút toán COGS: tính giá vốn từ SLE của DN (Σ valuation) → Dr 632 cogs / Cr 156 cogs. Set status "Đã phát hành", set delivery_note.status "Đã xuất HĐ". `on_cancel()`: `SCGLEntry.cancel_voucher("SC Sales Invoice", name)` + revert DN status.
- [ ] **Step 1 (RED):** `sc_sales_invoice_test.py`: `test_si_from_unaccepted_dn_blocked()` (BRU-DEL-001), `test_si_mismatch_dn_blocked()` (BRU-INVC-001), `test_si_posts_ar_gl()` (get_balance 131 party tăng grand_total; 511 credit), `test_si_cogs_posted()` (632 debit), `test_si_fiscal_lock_blocked()` (BRU-PAY-001), `test_si_cancel_reverses_gl()`. `run` FAIL.
- [ ] **Step 2 (GREEN):** tạo doctype + controller. migrate. `run` PASS.
- [ ] **Step 3:** migrate+ping. **Commit** `feat(mvl-sales): SC Sales Invoice — GL phải thu + COGS (BRU-DEL/INVC/PAY)`

---

### Task 8: SC Sales Receipt (thu tiền)
**Files:** Create `sc_sales_receipt`(submittable); Create `supplycore/tests/sc_sales_receipt_test.py`
SR fields: `receipt_no`, `customer`(reqd), `sales_invoice`(Link, reqd), `receipt_date`(Date, default Today), `amount`(Currency, reqd), `mode`(Select "Tiền mặt\nChuyển khoản", default "Chuyển khoản").
Controller `validate()`: amount ≤ sales_invoice.outstanding_amount (else throw); BRU-PAY-001 fiscal lock. `on_submit()`: ghi GL `post_journal`: Dr <cash/bank account theo mode> amount; Cr 131 (party=customer) amount. Giảm sales_invoice.outstanding_amount; cập nhật SI.status ("Đã thu một phần" nếu còn outstanding>0, "Đã thu đủ" nếu =0). `on_cancel()`: cancel_voucher + tăng lại outstanding + revert status.
- [ ] **Step 1 (RED):** `test_receipt_reduces_outstanding()`, `test_receipt_full_sets_paid()`, `test_receipt_over_outstanding_blocked()`, `test_receipt_posts_gl()` (131 giảm, cash tăng). `run` FAIL.
- [ ] **Step 2 (GREEN):** tạo doctype. migrate. `run` PASS.
- [ ] **Step 3:** migrate+ping. **Commit** `feat(mvl-sales): SC Sales Receipt — thu tiền tất toán phải thu`

---

### Task 9: API scm.* bán hàng + wiring role/permission
**Files:** Create `supplycore/api/sales.py`; Modify `supplycore/api/users.py` (role-guide M7 = "Bán hàng & Bàn giao"); Create `supplycore/tests/sales_api_test.py`
Whitelisted (mirror api hiện có, kiểm quyền→rule→ghi→audit):
`scm.customer.upsert`, `scm.sales_framework.create`, `scm.sales_order.approve`, `scm.delivery.create`, `scm.delivery.accept`, `scm.sales_invoice.create`, `scm.receipt.collect`. (Đặt trong `api/sales.py`; nếu naming `scm.*` cần alias theo cách app expose — mirror cách api/frontend.py/kpi.py được gọi.)
- [ ] **Step 1 (RED):** `sales_api_test.py`: gọi từng API tạo doc hợp lệ → trả tên doc; gọi với vi phạm rule → raise. `run` FAIL.
- [ ] **Step 2 (GREEN):** viết api/sales.py, cập nhật users.py role-guide. migrate. `run` PASS.
- [ ] **Step 3:** migrate+ping. **Commit** `feat(mvl-sales): API scm.* bán hàng + role-guide M7 Sales`

---

### Task 10: E2E Order-to-Cash + seed demo + regression
**Files:** Create `supplycore/tests/sales_e2e_test.py`; Optional: `supplycore/setup/seed_sales_demo.py` (customer + SFC demo tối thiểu)
- [ ] **Step 1:** `sales_e2e_test.py::test_full_o2c()`: tạo Customer → SFC (submit) → SO (approve) → DN (submit, SLE âm) → Acceptance (submit) → SI (submit, GL 131 tăng) → Receipt (submit, 131 về 0, SI "Đã thu đủ"). Assert số dư sổ kho & sổ cái đúng ở từng bước.
- [ ] **Step 2:** chạy `run` → PASS.
- [ ] **Step 3 (regression):** `bench migrate` + `bench build --app supplycore` sạch; ping pong; chạy lại vài smoke test M1-M6/M8-M11 hiện có (smoke_m10, uc26) → vẫn pass; grep tiếng Anh hospital vẫn = 0.
- [ ] **Step 4: Commit** `test(mvl-sales): E2E Order-to-Cash + seed demo + regression GĐ2`

---

## Định nghĩa Done (GĐ2)
- [ ] 7 DocType bán + 4 child + module M7 Sales, migrate/build/ping sạch.
- [ ] 11 BRU (mục 6 spec) có test đỏ→xanh; E2E O2C pass.
- [ ] SLE âm khi giao; GL phải thu khi xuất HĐ; GL tất toán khi thu tiền — số dư khớp.
- [ ] Không hồi quy GĐ1. Commit trên `feat/mvl-distributor`.
- [ ] Sẵn sàng GĐ3 (Portal M12): SC Customer.portal_user, cô lập dữ liệu, 4 cột mốc.
